github:
  api:
    base-url: https://api.github.com
    token: ${GITHUB_TOKEN}

spring:
  main:
    banner-mode: off

logging:
  level:
    root: INFO
  file:
    name: logs/app.log

server:
  port: 8080